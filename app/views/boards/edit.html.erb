<%= form_for @board do |f|%>
  <%= f.label :name %>
  <%= f.text_field :name, value: @board.name %>
  <%= f.label :description %>
  <%= f.text_field :description, value: @board.description %>
  <%= f.submit "Update Board" %>
<% end %>
